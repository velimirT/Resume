/**
 *
 * StyledCertificates
 *
 */

// import PropTypes from 'prop-types';
import styled from 'styled-components';

const StyledCertificates = styled.section`
  & img {
    height: 100%;
    margin-right: 20px;
  }
`;

export default StyledCertificates;
